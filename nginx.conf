user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
  worker_connections 1024;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                  '$status $body_bytes_sent "$http_referer" '
                  '"$http_user_agent" "$http_x_forwarded_for"';

  access_log /var/log/nginx/access.log main;

  sendfile on;
  tcp_nopush on;
  keepalive_timeout 65;
  gzip on;

  server {
    listen 3000;
    server_name _;
    root /usr/share/nginx/html;

    # SPA routing: all non-asset requests -> index.html
    location / {
      try_files $uri $uri/ /index.html;
    }

    # API proxy to backend (set via env var or hardcoded for prod)
    # Trailing slash on proxy_pass strips the /api/ prefix so backend
    # routes stay at /query, /auth/*, etc.
    location /api/ {
      proxy_pass $API_BACKEND_URL/;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;

      # SSE streaming (/query/stream) must reach the client as it arrives
      proxy_http_version 1.1;
      proxy_set_header Connection '';
      proxy_buffering off;
    }

    # Cache static assets (JS, CSS, images)
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
      expires 1y;
      add_header Cache-Control "public, immutable";
    }
  }
}
