MONGODB_URI=
MONGODB_DB_NAME=
MONGODB_COLLECTION_CHUNKS=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
OPENROUTER_GENERATION_MODEL=
OPENROUTER_REWRITER_MODEL=
OPENROUTER_EVAL_JUDGE_MODEL=
FRONTEND_ORIGIN=http://localhost:5174
VITE_API_URL=http://localhost:8000
POSTGRES_URI=
JWT_SECRET=
JWT_EXPIRY_MINUTES=10080
EMAIL_PROVIDER=resend
# EMAIL_API_KEY and EMAIL_FROM require a verified sending domain in Resend.
# Not needed to run the app or test suite locally — send_otp_email is mocked in tests.
# Only required for manually testing real OTP email delivery before deploying.
EMAIL_API_KEY=
EMAIL_FROM=
OTP_TTL_MINUTES=10
OTP_MAX_ATTEMPTS=5
OTP_RESEND_COOLDOWN_SECONDS=60
OTP_CODE_LENGTH=6